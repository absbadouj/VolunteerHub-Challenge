const organisation = require("./organisation");
const user = require("./user");
const project = require("./project");
const address = require("./address");

module.exports = { organisation, user, project, address };