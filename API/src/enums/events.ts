enum EVENTS {
    CONNECTED = 'connected',
    DISCONNECTED = 'diconnected',
    ERROR = 'error',
    FINISH = 'finish',
    SIGINT = 'SIGINT',
    SIGTERM = 'SIGTERM',
    SIGUSR2 = 'SIGUSR2',
}

export default EVENTS;